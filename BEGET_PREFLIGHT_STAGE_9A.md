# Beget: предпроверка этапов 9A.2–9A.3

Проверка выполнена 30–31 августа 2026 года только в режиме чтения. Размещение и любые изменения в панели не выполнялись.

## 1. Антивирус

В панели показано 25 находок на пяти других сайтах аккаунта: `akppservice-spb.ru` — 2, `new1` — 20, `remont-akpp1.ru` — 1, `variator-msk.ru` — 1, `remontvariatora1.ru` — 1. Среди показанных заражённых путей отсутствует `/remontvariator.ru/public_html` и его `index.php`. Для другого сайта `remontvariatora1.ru` показан путь `public_html/wp-content/plugins/elementor-pro/elementor-pro.php` и статус «найден 1 вирус». Тип угрозы и дата недоступны: раздел ImunifyAV+ открывается пустым. Лечение, удаление и изменение файлов не выполнялись.

## 2. Почтовый ящик

Ящик `info@remontvariator.ru` существует: он показан в списке ящиков домена `remontvariator.ru`, содержит 1 сообщение и занимает 2,63 КБ. В панели не показано отдельных признаков блокировки или переполнения; отдельная метка активности не отображается. Письма, пароль и настройки ящика не открывались. Единственный согласованный получатель заявок — `info@remontvariator.ru`.

## 3. PHP

Текущая версия PHP для `remontvariator.ru` и перечень доступных версий не отображены. Раздел «Управление сайтами» подтверждает сайт и web-root, а «Управление услугами» после повторной загрузки отображает тариф, но не per-site параметры PHP; прямой маршрут `/php` возвращает 404. PHP 8.2+ не подтверждён. Для размещения требуется подтвердить PHP 8.2 или новее, `mail()`, `random_int`, JSON и возможность записи во временный каталог уже после загрузки тестового пакета.

## 4. Резервные копии

Автоматические копии доступны: «Файловый архив» содержит `remontvariator.ru`; последняя доступная фиксированная точка — `31.08.2026 02:05` (также отображается «Текущее состояние»). Панель сообщает, что можно восстановить все данные или выбрать отдельные файлы для восстановления; это включает директорию сайта. В панели есть отдельная вкладка «Backup по требованию», то есть возможность создания отдельной копии предусмотрена. Ничего не создавалось, не выгружалось и не восстанавливалось.

## 5. DNS и текущая публикация Tilda

Домен `remontvariator.ru` присутствует в аккаунте, а в панели существует DNS-зона Beget. Индивидуальные записи и фактическая внешняя делегация из видимого интерфейса не подтверждены. Внешние проверки `http://remontvariator.ru`, `https://remontvariator.ru` и `https://www.remontvariator.ru` отдают страницу Tilda с ошибкой 402. Это указывает, что внешняя маршрутизация домена всё ещё ведёт на Tilda; точную запись нужно установить перед 9Б без каких-либо изменений на этом этапе.

## 6. SSL и HTTP → HTTPS

При открытии HTTPS для основного домена и `www` браузер не показал предупреждение о небезопасном сертификате; срок действия и покрытие сертификата в панели не подтверждены. HTTP не перенаправляется на HTTPS и также показывает Tilda 402. Настройка редиректа и SSL не менялись.

## 7. Метрика

Поиск в исходнике и доступной истории Git не нашёл идентификатор существующего счётчика. Содержимое прежнего `public_html/index.php` безопасно не было предоставлено просмотрщиком панели, поэтому проверить его на код Метрики не удалось. Новый счётчик не создавался и не включался.

## 8. Известный web-root

Для `remontvariator.ru` в панели указан web-root `/remontvariator.ru/public_html`. В его верхнем уровне видны только `cgi-bin/` и `index.php`; содержимое файлов не менялось.

## 10. Решения перед 9Б

**Техническая конфигурация.** Почтовый ящик существует, а автоматические резервные копии и механизм восстановления доступны. Переход к безопасному staging пока запрещён, потому что не подтверждены текущая версия PHP и доступность PHP 8.2+.

**Безопасность аккаунта.** Переход к staging также запрещён: в одном аккаунте хостинга есть 25 неустранённых находок на других сайтах. Целевой `remontvariator.ru` не показан среди заражённых путей, но файлы сайтов находятся в одном аккаунте; до загрузки требуется решение владельца аккаунта или Beget по заражениям. 

Текущий PHP-обработчик заявок отправляет заявки только на `info@remontvariator.ru`; серверная доставка в Telegram удалена, а публичная ссылка Telegram остаётся контактом. Никакие файлы Beget, почта, PHP, DNS, SSL и настройки аккаунта не изменялись.
